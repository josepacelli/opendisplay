[package]
name = "tray"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "windows-tray: the unprivileged OpenDisplay Windows sender tray UI."

[dependencies]
# Added by T24 (ipc::connect client): the tray<->core message schema.
ipc = { path = "../ipc" }
