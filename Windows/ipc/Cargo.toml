[package]
name = "ipc"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Local-only named-pipe message schema between windows-tray and windows-core."

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
